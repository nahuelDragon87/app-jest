import { findDOMNode } from "react-dom";
var findNodeHandle = function(host) {
  var node;
  try {
    node = findDOMNode(host);
  } catch {
  }
  return node;
};
export {
  findNodeHandle
};
//# sourceMappingURL=findNodeHandle.js.map
